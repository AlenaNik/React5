import  React  from  'react';

const  GenerateQuote = ({ selectSimpson }) => {
    return (
        <div  className="GenerateQuote">
            <button  onClick={selectSimpson}>Hope it works</button>
        </div>
    );
};

export  default  GenerateQuote;
